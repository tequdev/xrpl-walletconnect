{
  "name": "root",
  "private": true,
  "scripts": {
    "build": "lerna run build",
    "version": "lerna version --no-private",
    "publish": "lerna publish --no-private"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^6.6.2"
  }
}
